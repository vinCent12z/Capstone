<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Smart Judging & Scoring System</title>
    <link rel="stylesheet" href="style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-slate-50 text-slate-800">
    <!-- ===== Header / Navigation ===== -->
    <header>
      <nav class="bg-gradient-to-r from-indigo-500 to-blue-500 shadow-md">
        <div
          class="max-w-7xl mx-auto px-4 py-4 flex flex-col md:flex-row md:items-center md:justify-between"
        >
          <h1 class="text-white text-2xl font-bold mb-4 md:mb-0">
            Smart Judge
          </h1>

          <!-- Button Navigation -->
          <div class="flex flex-wrap gap-3">
            <button
              class="bg-white/20 text-white px-4 py-2 rounded-full hover:bg-white/30 active:bg-white/40 transition"
              onclick="showSection('dashboard')"
            >
              Dashboard
            </button>
            <button
              class="bg-white/20 text-white px-4 py-2 rounded-full hover:bg-white/30 active:bg-white/40 transition"
              onclick="showSection('scoreboard')"
            >
              Score Board
            </button>
            
            <button
              class="bg-white/20 text-white px-4 py-2 rounded-full hover:bg-white/30 active:bg-white/40 transition"
              onclick="showSection('process')"
            >
              Process
            </button>

            <button
              class="bg-white/20 text-white px-4 py-2 rounded-full hover:bg-white/30 active:bg-white/40 transition"
              onclick="showSection('metrics')"
            >
              Metrics
            </button>
          </div>
        </div>
      </nav>
    </header>

    <!-- ===== Dashboard ===== -->
    <section id="dashboard" class="page max-w-6xl mx-auto p-6">
      <div
        class="flex flex-col items-center justify-center text-center py-16 px-6"
      >
        <h1 class="text-4xl md:text-6xl font-bold text-black mb-4">
          Real-Time Smart Judging &amp; Scoring
        </h1> 
        <p class="max-w-2xl text-lg text-black/80">
          Advanced AI-powered scoring system for events. Monitoring performance, analyze trends and optimize your event's efficiency in real-time.
        </p>
      </div>

      <h3 class="text-3xl font-semibold mb-6">Dashboard</h3>
      <div
        class="winner-box bg-yellow-100 border border-yellow-300 text-yellow-800 rounded-xl p-6 mb-8 shadow-sm"
      >
        <h3 id="winnerName" class="text-lg font-medium">No scores yet</h3>
        <p id="winnerScore">--</p>
      </div>

      <div class="overflow-x-auto">
        <table
          id="scoreTable"
          class="min-w-full bg-white rounded-xl shadow-md"
        >
          <thead class="bg-indigo-500 text-white">
            <tr>
              <th class="py-3 px-4 text-center">Name/Team</th>
              <th class="py-3 px-4 text-center">Performance</th>
              <th class="py-3 px-4 text-center">Style</th>
              <th class="py-3 px-4 text-center">Creativity</th>
              <th class="py-3 px-4 text-center">Rank</th>
            </tr>
          </thead>
          <tbody></tbody>
        </table>
      </div>
    </section>

    <!-- ===== Score Board ===== -->
<section id="scoreboard" class="page max-w-6xl mx-auto p-6 hidden">
  <h2 class="text-3xl font-semibold mb-6">Vote Tally</h2>
  <p class="text-gray-500 mb-4">
    Percentages based on total scores submitted.
  </p>

  <!--  Live Date/Time placeholder -->
  <div id="liveDateTime" class="text-gray-500 mb-4 font-medium text-center mb-8 hidden"></div>


  <div
    id="scoreboardContainer"
    class="grid grid-cols-2 md:grid-cols-4 gap-8 justify-items-center"
  ></div>
</section>


    <!-- ===== Process ===== -->
    <section id="process" class="page max-w-5xl mx-auto p-10 hidden">
      <div class="text-center mb-12">
        <h2
          class="text-5xl font-extrabold bg-gradient-to-r from-indigo-600 to-blue-500 bg-clip-text text-transparent"
        >
          Scoring Process
        </h2>
        <p class="mt-3 text-gray-500 text-lg">
          Add participants, assign scores, and view real-time logs
        </p>
      </div>

      <!-- Add Participant -->
      <div
        class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 mb-12 border border-white/40"
      >
        <h3 class="text-2xl font-semibold text-gray-800 mb-6">
          Add Team / Participant
        </h3>
        <div class="flex flex-col sm:flex-row gap-4">
          <input
            id="participantName"
            type="text"
            placeholder="Enter Name or Team"
            class="flex-1 p-4 rounded-xl border border-gray-300 bg-white/70 focus:outline-none focus:ring-4 focus:ring-indigo-300 transition"
          />
          <button
            onclick="addParticipant()"
            class="px-8 py-4 bg-gradient-to-r from-indigo-500 to-blue-500 text-white font-semibold rounded-xl shadow-lg hover:from-indigo-600 hover:to-blue-600 transition"
          >
            Add
          </button>
        </div>
        <ul
          id="participantsList"
          class="mt-6 divide-y divide-gray-200 text-gray-700"
        ></ul>
      </div>

      <!-- === Glass Card: Score Form === -->
      <div
        class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 mb-12 border border-white/40"
      >
        <h3 class="text-2xl font-semibold text-gray-800 mb-6">
          Assign Scores
        </h3>
        <p class="text-gray-500 mb-4">
          Select a participant and rate each category (0–100).
        </p>
        <select

          id="selectParticipant"
          class="w-full p-4 mb-8 rounded-xl border border-gray-300 bg-white/70 focus:outline-none focus:ring-4 focus:ring-indigo-300 transition"
        ></select>
      </div>

      <div class="grid md:grid-cols-3 gap-6">
        <label class="flex flex-col">
          <span class="font-medium text-gray-700 mb-2">Performance</span>
          <input
            id="perfScore"
            type="number"
            min="0"
            max="100"
            class="p-4 rounded-xl border border-gray-300 bg-white/70 focus:outline-none focus:ring-4 focus:ring-indigo-300 transition"
          />
        </label>
        <label class="flex flex-col">
          <span class="font-medium text-gray-700 mb-2">Style</span>
          <input
            id="styleScore"
            type="number"
            min="0"
            max="100"
            class="p-4 rounded-xl border border-gray-300 bg-white/70 focus:outline-none focus:ring-4 focus:ring-indigo-300 transition"
          />
        </label>
        <label class="flex flex-col">
          <span class="font-medium text-gray-700 mb-2">Creativity</span>
          <input
            id="creativityScore"
            type="number"
            min="0"
            max="100"
            class="p-4 rounded-xl border border-gray-300 bg-white/70 focus:outline-none focus:ring-4 focus:ring-indigo-300 transition"
          />
        </label>
      </div>

      <button
        onclick="addCriteriaScores()"
        class="mt-8 px-10 py-4 bg-gradient-to-r from-indigo-500 to-blue-500 text-white font-semibold rounded-xl shadow-lg hover:from-indigo-600 hover:to-blue-600 transition"
      >
        Submit Scores
      </button>

      <!-- Logs -->
      <div
        class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 border border-white/40 mt-12"
      >
        <h3 class="text-2xl font-semibold text-indigo-600 mb-4">Score Logs</h3>
        <ul id="logsList" class="space-y-3 text-gray-800"></ul>
      </div>
    </section>

    <!-- ===== Metrics ===== -->
    <section id="metrics" class="page max-w-6xl mx-auto p-10 hidden">
      <div class="text-center mb-12">
        <h2
          class="text-5xl font-extrabold bg-gradient-to-r from-indigo-600 to-blue-500 bg-clip-text text-transparent"
        >
          Event Metrics
        </h2>
        <p class="mt-3 text-gray-500 text-lg">
          Real-time statistics of participants and scoring
        </p>
      </div>

      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
        <div
          class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 border border-white/40 text-center"
        >
          <h3 class="text-lg font-semibold text-gray-700 mb-2">
            Total Participants
          </h3>
          <p id="metricParticipants" class="text-5xl font-extrabold text-indigo-600">
            0
          </p>
        </div>

        <div
          class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 border border-white/40 text-center"
        >
          <h3 class="text-lg font-semibold text-gray-700 mb-2">
            Total Scores Submitted
          </h3>
          <p id="metricScores" class="text-5xl font-extrabold text-indigo-600">
            0
          </p>
        </div>

        <div
          class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 border border-white/40 text-center"
        >
          <h3 class="text-lg font-semibold text-gray-700 mb-2">
            Highest Score
          </h3>
          <p id="metricHighest" class="text-5xl font-extrabold text-green-600">
          </p>
        </div>

        <div
          class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 border border-white/40 text-center"
        >
          <h3 class="text-lg font-semibold text-gray-700 mb-2">
            Lowest Score
          </h3>
          <p id="metricLowest" class="text-5xl font-extrabold text-red-600">
          </p>
        </div>

        <div
          class="backdrop-blur-md bg-white/70 rounded-3xl shadow-xl p-8 border border-white/40 text-center"
        >
          <h3 class="text-lg font-semibold text-gray-700 mb-2">
            Average Score
          </h3>
          <p id="metricAverage" class="text-5xl font-extrabold text-blue-600">
          </p>
        </div>
      </div>
    </section>

    <script src="script.js"></script>
  </body>
</html>
